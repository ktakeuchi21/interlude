import type { Lesson } from "./content";
import { foundationSources as s, foundationReview as review } from "./foundation-sources";

export const aiDevelopmentLessons: Lesson[] = [
  {
    id: "frontend-backend-data", version: 1, courseId: "ai-development", order: 1, title: "Frontend, backend, and data", minutes: 5,
    objective: "Trace a user action through the interface, server, and storage so you can specify what an AI coding assistant should build.",
    sections: [
      { title: "Follow one saved note", sources: [s.client.id], paragraphs: ["Imagine a small learning app called Fieldnotes. You type an observation on your phone, press Save, and expect to find it on your laptop. This fictional example will connect the software course. MDN describes a web interaction as a client sending a request and a server returning a response, often after working with stored data. For our note, the visible text box is only the beginning. Something must accept the request, decide whether it is allowed, save the information, and tell the interface what happened. Drawing that journey reveals requirements that an attractive screen alone cannot satisfy."] },
      { title: "The interface has a temporary memory", sources: [], paragraphs: ["The frontend is the part you interact with: the text box, Save button, loading message, and list of notes. While you type, the browser can hold a draft in memory. Seeing those words on screen does not prove that another device can retrieve them. Closing the page may discard a draft unless the application deliberately preserves it. In our design, distinguish three visible conditions: editing, saving, and saved. The saved label should correspond to the server confirming the write. If the connection fails, keep the draft and explain that it still needs saving. That is a behavior we can ask an engineer to demonstrate."] },
      { title: "The server enforces the rules", sources: [s.access.id], paragraphs: ["The backend receives work that should not depend on trusting the screen. OWASP recommends checking permissions on every request and denying access when permission is absent. For Fieldnotes, the server determines who is signed in and whether that person may write this note. Hiding another person's notes in the interface is insufficient if the server would still return them when asked. The server also checks the input: perhaps an empty note is invalid, and an unusually large note should receive a clear limit message. These are ordinary software rules. An AI model is not required to decide who owns a record."] },
      { title: "Storage outlives a page", sources: [], paragraphs: ["In our example, a database stores each note with a stable identifier, an owner, its text, and an update time. The identifier lets the app refer to the same note after its title changes. A separate object store might hold a large audio recording, while the database records where it belongs. These are possible architecture choices, not requirements for every application. Ask which information must survive closing the page, which must appear across devices, and which is temporary. Those answers help distinguish a personal browser preference from an authoritative record. They also make it easier to spot a demo that only pretends to save."] },
      { title: "Diagnose the boundary", sources: [], paragraphs: ["Suppose Save appears to work, but the laptop shows nothing. Several explanations remain possible: the phone never sent the request, the server rejected it, the write failed, or the laptop loaded an old list. Asking the assistant to fix the database immediately would choose a cause before collecting evidence. Instead, describe the exact actions and the expected result. Ask it to trace the request and report where reality diverges. A useful investigation can show a successful server response and then check the stored record. Avoid including private note text in diagnostic logs when a request identifier and status would answer the question."] },
      { title: "Describe the whole promise", sources: [], paragraphs: ["A useful feature request might say: after an authorized owner saves a valid note, reopening Fieldnotes on another signed-in device shows the same text; a failed save retains the draft and offers a retry. That sentence spans the interface, backend, and storage without prescribing every implementation detail. Now you can review an AI assistant's plan against a real outcome. You can also ask it to name the files that own each step. As you learn more software vocabulary, keep returning to the journey of one user action. Architecture becomes easier to understand when each part has a visible responsibility and a testable result."] },
    ],
    takeaway: "Trace the complete action: the interface collects input, the server enforces rules, and durable storage makes saved work available later.",
    reflection: "Which part of an app you use proves that a save succeeded across devices?",
    challenge: "Optional · 5 minutes: draw the path of a saved note. Label the draft, request, permission check, stored record, and confirmation.",
    quiz: { question: "A prototype displays a new note immediately. What best establishes that it is durably saved?", options: ["The Save button changes color", "The authorized server confirms storage and a fresh session can retrieve the note", "The text remains visible until the tab closes"], answer: 1, explanation: "Visible browser state can be temporary. Verify the saved record through a fresh read, with ownership enforced by the server." },
    sources: [s.client, s.access], review,
  },
  {
    id: "apis-application-state", version: 1, courseId: "ai-development", order: 2, title: "APIs and application state", minutes: 5,
    objective: "Specify a save operation that handles retries and conflicting edits without silently duplicating or losing work.",
    sections: [
      { title: "A request is an agreement", sources: [s.http.id], paragraphs: ["Fieldnotes now saves notes across devices. The next challenge is making that behavior dependable when the connection is awkward. An API is an interface through which one part of the system asks another to do something. In our fictional app, a save request identifies the note and supplies its new text; the response reports the saved result or a useful error. MDN explains that HTTP itself is stateless, while mechanisms such as cookies can support sessions. The practical consequence is that a server needs enough information and authentication context to handle each request. It cannot rely on the user having visited a particular screen first."] },
      { title: "Name the states explicitly", sources: [], paragraphs: ["Suppose you edit a note on a train. The app shows a draft, begins saving, and then loses connectivity. Has the note been saved? The honest answer may be unknown: the server could have stored it before the response was lost. Design separate states for unsaved changes, a pending request, confirmed success, and a recoverable failure. Avoid translating every timeout into a definite claim that nothing happened. The user should keep their text and have a way to reconcile the saved record. Thinking in states helps you ask for an understandable recovery path instead of merely asking for a faster or more reassuring spinner."] },
      { title: "Make repetition safe", sources: [s.retry.id], paragraphs: ["MDN defines idempotency in terms of repeated requests having the same intended effect on the server as one request. It does not require every response to be identical. Apply that idea to our save operation: assign a stable identifier to the intended change and let the server recognize a repeated submission. If the first attempt succeeded but its response disappeared, retrying should recover that result rather than create a second note. This is a proposed application design, not a claim that adding a particular HTTP method automatically prevents duplication. The storage rules must actually enforce the intended behavior under concurrent requests."] },
      { title: "Handle two writers", sources: [], paragraphs: ["Now imagine the phone and laptop both opened revision four of a note. The phone saves revision five. The laptop still has older text and tries to save its own changes. If every write silently replaces the current record, one device can erase the other's work. A possible design includes the revision that each editor started from. The server accepts a matching revision and rejects a stale one with a conflict response. The interface can then preserve the draft and show the newer saved text. This is a product choice: some simple preferences tolerate the latest write winning, while carefully written notes may deserve explicit conflict handling."] },
      { title: "Specify the unhappy cases", sources: [], paragraphs: ["An API contract should explain errors as well as success. For Fieldnotes, distinguish invalid text, missing permission, an outdated revision, and temporary service unavailability. Those outcomes call for different user actions. Retrying malformed input unchanged is unlikely to help; retrying an uncertain save with a stable operation identifier might. Ask the AI coding assistant to show a short example request and each relevant response before implementing the screen. Then walk through a duplicate click, a lost response, and an edit from a second device. You are testing the agreement between parts of the system, not just whether the normal button press appears to work."] },
      { title: "Choose a trustworthy confirmation", sources: [], paragraphs: ["For this feature, define saved as the server returning the note identifier and current revision after the write is recorded. The interface can then update its view from that result. If a request is uncertain, it can check the operation or reload the authoritative record before deciding what to do next. These choices add a little design work, but they remove guesswork from recovery. You do not need to memorize every HTTP status code to contribute. Ask what the request means, how the server recognizes repetition, and what happens when another device changes the same thing. Those questions expose the behavior users will depend on."] },
    ],
    takeaway: "A dependable API defines success, uncertainty, repetition, and conflicts so the app can recover without inventing an outcome.",
    reflection: "Where have you tapped a button twice because you could not tell whether the first attempt worked?",
    challenge: "Optional · 5 minutes: specify a note save, including its operation identifier, revision, success response, and one conflict message.",
    quiz: { question: "The server saved a note, but the response was lost. What should a safe retry do?", options: ["Always create a new note", "Assume the server failed and erase the draft", "Recognize the same intended operation and return its existing result"], answer: 2, explanation: "The request's outcome is uncertain. A stable operation identity and enforced storage rules can prevent a retry from duplicating the action." },
    sources: [s.http, s.retry], review,
  },
  {
    id: "useful-ai-coding-context", version: 1, courseId: "ai-development", order: 3, title: "Giving an AI assistant useful context", minutes: 5,
    objective: "Write a coding request with a concrete outcome, relevant evidence, and boundaries the assistant can verify.",
    sections: [
      { title: "Replace the vague request", sources: [s.context.id], paragraphs: ["Imagine asking an AI assistant to make Fieldnotes better. It could redesign the homepage, add tags, or rewrite the backend, and still believe it followed your request. GitHub's prompting guidance recommends supplying a goal, specific requirements, examples, and relevant context. Apply that to a smaller task: let the owner search saved notes by text. Describe the moment that matters: after entering a phrase, the user sees matching notes and can open one without losing the query. A concrete before-and-after example gives the assistant something to implement and gives you something to judge. It also makes disagreements about scope easier to identify early."] },
      { title: "Supply the evidence it needs", sources: [], paragraphs: ["Useful context is selective. Point to the current notes list, the route that reads notes, and an existing search pattern if the project has one. Include the framework and package versions when an implementation depends on them, preferably by asking the assistant to inspect the actual project files. Explain whether search should include archived notes and whether matching is case sensitive. An example can remove ambiguity: searching for train should find a note containing Train journey. Avoid sending a huge unrelated document dump and assuming the important detail will receive attention. The assistant needs to know which facts are authoritative and which are merely your initial ideas."] },
      { title: "Separate goals from constraints", sources: [], paragraphs: ["The goal is the user outcome. Constraints define the space in which it should be achieved. For our fictional search, the owner must only see their own notes, existing saved text must remain intact, and no new external search service is needed for this small library. You might also ask the assistant to preserve the current visual style. Explain why a constraint matters when the reason affects tradeoffs. For example, avoiding a new paid service keeps this personal project simple to operate. A list of preferred technologies without a clear outcome is not a substitute for a feature description; both kinds of information serve different purposes."] },
      { title: "Request a traceable plan", sources: [], paragraphs: ["Before a complex change, ask the assistant to describe the smallest end-to-end implementation and identify the files it expects to touch. A useful plan connects each proposed edit to a requirement. The search input belongs in the list view; owner-scoped matching belongs in the data access path; the empty result needs a visible message. If the plan includes replacing authentication, ask what requirement makes that necessary. This is a way to discover misunderstandings while they are cheap to correct. For a tiny reversible edit, a long planning ceremony would add little value. Scale the planning effort to the uncertainty and consequences of the change."] },
      { title: "Treat the explanation as a hypothesis", sources: [s.responsible.id], paragraphs: ["GitHub notes that generated code can be inaccurate or insecure and recommends reviewing and testing it. The same caution helps with an assistant's confident explanation of your project. Ask it to point to the code it inspected and distinguish observed behavior from assumptions. If it says search already exists, have it identify the entry point and show how a user reaches it. If it reports that tests passed, check which tests ran and what they cover. You can ask the assistant for supporting evidence without demanding a transcript of every thought. What matters is whether its claims can be checked against files, outputs, and the running application."] },
      { title: "Close the loop with a specific correction", sources: [], paragraphs: ["Suppose the first implementation only searches note titles, but your example expected matches inside note text. Respond with the concrete mismatch: the query train should find the sentence about a train journey in the note body; currently it does not. That is more useful than saying the result feels wrong. Ask for the narrow correction and an appropriate verification. Then keep the clarified behavior in the feature description so a later edit does not undo it. Effective collaboration with an AI coding assistant is an evidence loop: describe an outcome, inspect the proposed change, observe its behavior, and refine the requirement where reality exposes an ambiguity."] },
    ],
    takeaway: "Give the assistant an outcome, relevant project evidence, constraints, and examples; judge the result through observable behavior.",
    reflection: "Which ambiguity in a recent request could you replace with a concrete input and expected output?",
    challenge: "Optional · 5 minutes: write a search-feature request with one example, two constraints, relevant files to inspect, and an acceptance check.",
    quiz: { question: "Which request gives a coding assistant the most useful starting point?", options: ["Improve the app however you think best", "Add owner-only search across note text; the query train must match Train journey; inspect the existing list and data route", "Use the newest framework and make it intelligent"], answer: 1, explanation: "The request provides a specific behavior, an example, a permission boundary, and relevant context to inspect." },
    sources: [s.context, s.responsible], review,
  },
  {
    id: "small-reviewable-changes", version: 1, courseId: "ai-development", order: 4, title: "Small, reviewable changes", minutes: 5,
    objective: "Break a feature into coherent changes that can be reviewed and recovered without mixing unrelated work.",
    sections: [
      { title: "Keep one reason for the change", sources: [s.small.id], paragraphs: ["Your AI assistant can produce code quickly. That makes the size of the change a design decision you should make deliberately. Google's engineering guidance recommends small, self-contained changes with their related tests. Small means a coherent purpose, not a magical line count. In Fieldnotes, suppose the requested feature is archiving a note. An implementation that also replaces authentication, reformats every file, and adds a dashboard creates several independent reasons the app might behave differently. Even if each idea has merit, combining them makes the archive feature harder to assess. Begin by naming the one user-visible behavior this change is intended to introduce."] },
      { title: "Find a complete slice", sources: [], paragraphs: ["A useful slice of archiving could include the database field, an owner-checked archive action, a control in the notes list, and a way to view archived notes. Those pieces cross technical layers, but together they express one usable behavior. By contrast, adding a button that cannot save anything is visually small and functionally incomplete. Ask what a reviewer can demonstrate after the change. If a feature is too large for one review, divide it into stages that leave the application working at each point. A preparatory data change can be separate when it is compatible with the current application and its purpose is clearly explained."] },
      { title: "Separate cleanup when it hides the behavior", sources: [], paragraphs: ["Imagine the assistant renames every component while adding Archive. The resulting diff, meaning the comparison between old and new source, is dominated by movement rather than behavior. A reviewer must work harder to find the lines that actually change access or storage. Some local cleanup may make the feature easier to understand, and a necessary refactor can be part of the work. The question is whether the cleanup supports this change and can be reviewed with it. Ask the assistant to explain the relationship. When the explanation is simply that the old code looked untidy, consider keeping that separate from the feature you are trying to validate."] },
      { title: "Make the review understandable", sources: [s.review.id], paragraphs: ["Google's review guidance covers design, functionality, complexity, and tests. For our archive change, the reviewer needs the behavioral rule before inspecting details: archiving removes a note from the active list but keeps its text and provides a restore path. Describe what happens to an already open note and how search treats archived items. Then the reviewer can ask whether the implementation matches the promise. A concise change description should identify the problem, resulting behavior, and meaningful validation. It should not require the reviewer to reconstruct the original chat or guess which of several unrelated edits is the important one."] },
      { title: "Plan for recovery before release", sources: [], paragraphs: ["Smaller changes can make recovery easier, but undoing source code does not necessarily undo stored data. In our example, a new archive flag can be designed so the previous application safely ignores it. A destructive migration that deletes note text would create a very different recovery problem. Ask what remains compatible if the new interface is rolled back and whether any data operation is irreversible. For this feature, preserving the note and changing its visibility is a practical design choice. The point is not to demand a complicated rollback procedure for every small edit. It is to understand the consequences of the state your feature changes."] },
      { title: "Use a stopping rule", sources: [], paragraphs: ["Once the archive behavior works, the ownership checks hold, and the relevant validation passes, stop expanding the change. A new idea for automatic categorization can become a later task. This protects your ability to learn from a release: if users cannot find archived notes, you know which behavior to investigate. Ask an AI assistant to report unexpected adjacent problems without automatically absorbing all of them into the current feature. You remain responsible for deciding which work belongs together. The practical habit is to keep a change small enough that someone can explain its purpose, inspect its consequences, and decide whether it is ready with confidence."] },
    ],
    takeaway: "Organize a change around one coherent behavior, include the necessary layers, and understand how its data changes can be recovered.",
    reflection: "Where could a seemingly helpful cleanup make a feature harder to review?",
    challenge: "Optional · 5 minutes: split an archive feature into a smallest usable change and two explicitly deferred ideas. Describe recovery for its stored data.",
    quiz: { question: "An archive feature changes the database, API, and screen. Is that automatically too large?", options: ["Yes, each file must always be released separately", "No, those changes can form one coherent, reviewable behavior", "No, so unrelated authentication changes should be included too"], answer: 1, explanation: "Coherence matters more than file count. Include what makes the behavior complete, while separating unrelated changes that obscure review." },
    sources: [s.small, s.review], review,
  },
  {
    id: "reviewing-generated-code", version: 1, courseId: "ai-development", order: 5, title: "Reviewing generated code", minutes: 5,
    objective: "Review an AI-generated change by tracing behavior, permission boundaries, and failure cases instead of relying on its confident explanation.",
    sections: [
      { title: "Start with the promise", sources: [s.review.id], paragraphs: ["An assistant has implemented Fieldnotes search. Before reading every line, restate the promise: the owner searches their saved note text, sees relevant matches, and opens a result. Google's engineering review guidance starts with overall design and also considers functionality, complexity, and tests. Use that structure to orient your review. Identify the input, the code that reads stored notes, and the result shown to the user. If you cannot tell where the ownership restriction is enforced, that is a question to resolve before approving the change. A clear walkthrough is more useful than a general assurance that the implementation follows best practices."] },
      { title: "Read the important boundaries", sources: [], paragraphs: ["For our fictional feature, focus first on the places where information crosses a boundary. What enters from the browser? What determines whose notes are queried? How does the database receive the search text? What leaves the server? You do not have to become an expert in every library to ask for those answers. Have the assistant point to the relevant code and explain it in terms of the feature. Ask what happens when the search text is empty, very long, or contains punctuation. The goal is to understand the actual behavior, including how untrusted input is handled, rather than judging whether the code looks polished."] },
      { title: "Check omissions as well as additions", sources: [], paragraphs: ["A diff shows changed lines, but a missing behavior may appear nowhere. Suppose the search works beautifully on active notes, while archived notes disappear from all searches without any stated decision. Or suppose the server now returns results in an unpredictable order, so the same query feels unstable. Compare the implementation against the feature description and existing behavior. Ask whether the change affects an adjacent workflow the user depends on. This does not mean expanding the feature to cover every imaginable possibility. It means identifying the assumptions the implementation has made and deciding whether they agree with the intended scope before those assumptions become surprising product behavior."] },
      { title: "Use AI review as another signal", sources: [s.responsible.id], paragraphs: ["GitHub explains that AI code review can miss problems and can report problems that are not real. Generated fixes can also be incorrect. A second AI pass can help you find questions to investigate, but a clean report is not proof of correctness. When a reviewer flags a bug, ask for the concrete input or sequence that produces it. If the assistant proposes a fix, check that it addresses the demonstrated issue without weakening another requirement. In our search example, removing a permission filter might make missing results appear, but it would solve the wrong problem. A convincing explanation still needs to match the code and observed behavior."] },
      { title: "Ask for evidence proportional to the change", sources: [], paragraphs: ["A text-label adjustment may need only a visual check. Search touches input handling, data access, and navigation, so it deserves more evidence. Try a known matching phrase, a phrase with no result, and an unauthorized request in an appropriate test environment. Verify that opening a result preserves the search state if that is part of the promise. Ask which automated checks cover the behavior and which parts were inspected manually. Passing tests are useful within their coverage; they do not establish that every requirement was tested. Keep the evidence focused on the risks introduced by this change rather than demanding an impressive but irrelevant checklist."] },
      { title: "Make a clear review decision", sources: [], paragraphs: ["Finish the review by separating blocking issues from optional improvements. An ownership leak blocks release. A suggestion to rename an understandable helper may be optional. For each required correction, state the observed behavior, expected behavior, and why the difference matters. That gives the author a concrete target and lets you verify the next revision efficiently. If you lack the expertise to assess a sensitive part, identify that gap and involve someone qualified instead of disguising uncertainty as approval. Good review is a decision supported by evidence. The assistant can accelerate investigation, but the person accepting the change still needs to understand what has been accepted."] },
    ],
    takeaway: "Review the promised behavior and its boundaries, then use concrete evidence to resolve both human and AI review findings.",
    reflection: "What would you need to see before trusting a generated change that handles private data?",
    challenge: "Optional · 5 minutes: review a small diff or feature plan. Write one behavior check, one boundary check, and one concrete failure case.",
    quiz: { question: "An AI reviewer reports no issues. What does that establish?", options: ["The change has no defects", "It is one review signal; important behavior and boundaries still need evidence", "Human review is now unnecessary"], answer: 1, explanation: "AI review can miss defects or misunderstand code. Acceptance should rest on the actual change and validation relevant to its consequences." },
    sources: [s.review, s.responsible], review,
  },
  {
    id: "tests-debugging", version: 1, courseId: "ai-development", order: 6, title: "Tests and debugging", minutes: 5,
    objective: "Turn a reported bug into a reproducible case and choose tests that protect the behavior the user actually needs.",
    sections: [
      { title: "Make the failure reproducible", sources: [], paragraphs: ["A Fieldnotes user reports that search sometimes loses their query. Before asking an AI assistant to rewrite the search component, describe a precise sequence: type train, open a result, return to the list, and observe an empty search box. State the expected result: the query and matching results should remain. Include relevant conditions, such as whether this happens after using the browser Back button or the app's own navigation. A reproducible case narrows the problem and gives you a way to judge a fix. Without it, an assistant can make plausible edits while neither of you knows whether the original failure has actually been addressed."] },
      { title: "Test what matters to the user", sources: [s.testing.id], paragraphs: ["Playwright's testing guidance recommends checking user-visible behavior and isolating tests from one another. For our example, a browser test can type the query, open a note, navigate back, and verify that the query remains. That captures the promise more directly than checking whether a particular internal function was called. A smaller unit test can still be valuable for a pure matching rule, such as case-insensitive comparison. Choose the level that exposes the failure clearly. Tests at different levels answer different questions; a fast function test does not establish that navigation works, and a broad browser test may be unnecessary for every small calculation."] },
      { title: "Find the first wrong assumption", sources: [], paragraphs: ["Now trace what happens when the result opens. Perhaps the list component is removed and its local query state disappears. Perhaps the query is written to the URL but the return link drops it. Those are hypotheses to inspect, not facts about every web framework. Ask the assistant to show where the query is created, preserved, and restored. Compare each observation with the expected sequence. Diagnostic output should help distinguish competing explanations; adding random logs everywhere creates noise. Once you find the first point where the state is lost, a small correction is usually easier to reason about than rewriting every component involved in the journey."] },
      { title: "Separate a product defect from a fragile test", sources: [], paragraphs: ["Suppose the browser test fails because it looks for an element by a decorative CSS class that changed. The product may still work. Conversely, a test that always waits a fixed five seconds might pass on your machine and fail on a slower one. Inspect the failure before changing either the test or application. In our example, locate the search field by its accessible label and wait for the expected result rather than guessing how long rendering takes. These are practical applications of testing a stable user contract. Do not weaken an assertion merely to get a green result; first establish whether the assertion still expresses the intended behavior."] },
      { title: "Challenge the fix", sources: [s.responsible.id], paragraphs: ["GitHub's responsible-use guidance says generated code needs review and testing. Apply that to a proposed bug fix by running the original reproduction and checking a nearby case. If preserving the query in the URL solves Back navigation, does opening the page directly with that URL also work? Does clearing the query remove the filter? Those checks relate to the changed behavior. You need not test the entire internet every time a small fix lands. Ask the assistant to explain what the correction changes and why it should address the identified cause. The explanation should connect to evidence from the reproduction, not just a familiar-sounding pattern."] },
      { title: "Keep a useful regression check", sources: [], paragraphs: ["A regression test records an important behavior so a future change does not quietly break it again. For the lost-query bug, retain the navigation scenario if it is a workflow you expect to maintain. Keep its data isolated so an unrelated test cannot make it pass or fail by leaving notes behind. After the relevant checks pass, report the result plainly: the query now survives returning from a note, the original sequence passes, and direct-link behavior was also checked. If a device-specific behavior remains untested, say so. Good debugging ends with a demonstrated correction and a clear account of the evidence, rather than a large pile of speculative changes."] },
    ],
    takeaway: "Reproduce the failure, locate where behavior diverges, make a targeted correction, and retain a meaningful check against regression.",
    reflection: "What details would make your next bug report reproducible by someone who has never seen the problem?",
    challenge: "Optional · 5 minutes: write a bug report with starting state, exact actions, observed result, expected result, and one verification after the fix.",
    quiz: { question: "A test fails after a visual refactor because a CSS class changed, but the user flow still works. What should you do first?", options: ["Remove all assertions", "Investigate the selector and preserve an assertion of the actual user behavior", "Rewrite the storage layer"], answer: 1, explanation: "Determine whether the test or product is wrong. Prefer a stable user-facing contract while keeping the meaningful behavior check." },
    sources: [s.testing, s.responsible], review,
  },
  {
    id: "secrets-access-deployment", version: 1, courseId: "ai-development", order: 7, title: "Secrets, access, and deployment", minutes: 5,
    objective: "Explain where credentials and permission checks belong, then verify a release without exposing private data or secrets.",
    sections: [
      { title: "A working demo has more than one audience", sources: [], paragraphs: ["Fieldnotes is ready for a small private release. You can open it on your laptop, but that alone does not establish who else can access it or where its credentials are stored. A deployment makes a particular application version available in an environment with its own configuration and data. The same code can behave differently if a secret is missing, a database binding points elsewhere, or an access policy is wrong. Before release, name the intended audience and the environment. For this fictional project, the audience is one owner, and the application should reject everyone else at the server even if they discover its address."] },
      { title: "Keep service credentials out of the browser", sources: [s.secrets.id], paragraphs: ["OWASP recommends managed secret storage, limited access, and a supported rotation process. For Fieldnotes, a credential used to call a paid transcription provider belongs in the server's secret configuration. Code sent to the browser can be inspected by the person running it, so hiding a key in a frontend variable is not protection. The browser asks our server to perform an authorized operation; the server uses the credential without returning it. Also consider logs, screenshots, and copied configuration snippets. A secret can escape through those paths even when it is absent from source code. Review diagnostic output before sharing it with an assistant or another person."] },
      { title: "Identity is different from permission", sources: [s.access.id], paragraphs: ["Authentication establishes an identity; authorization determines what that identity may do. OWASP recommends least privilege and server-side enforcement. In our example, recognizing a signed-in person does not mean they own Fieldnotes. The backend should compare that identity with the allowed owner before reading notes, exporting data, or starting a paid operation. A disabled button helps explain the interface but is not the permission check. Similarly, the transcription service credential should have only the access it needs. Separate the user's right to request a task from the server's ability to call the provider. Both boundaries need deliberate configuration and verification."] },
      { title: "Treat configuration as part of the release", sources: [], paragraphs: ["A useful release record identifies the source version, required configuration names, database changes, and intended access policy. It contains secret names, never their values. Confirm whether changing a configuration value takes effect immediately or requires a new deployment in the hosting system you actually use. Avoid assuming that saving a secret has already changed the running application. For Fieldnotes, a missing transcription credential should produce an understandable unavailable state while saved notes continue working. That gives the owner a clear next step and helps distinguish a configuration problem from a defect in the note-taking feature."] },
      { title: "Verify both allowed and denied behavior", sources: [], paragraphs: ["After deployment, test the owner path and an appropriate signed-out or unauthorized path. The owner should read and save a note; the unauthorized request should not receive private content. Check that the deployed version is the one you validated and that it uses the intended storage. A visual success screen is not enough if the backend returns sensitive information to the wrong caller. For a paid feature, verify a bounded sample and observe the provider usage record before scaling up. These are proposed release checks for our small app. A larger organization may require additional controls, review roles, and environment-specific procedures."] },
      { title: "Know how to change a compromised credential", sources: [], paragraphs: ["If a credential is exposed, removing it from the latest source file does not make the old value safe. It may remain in history or a copied log. The operating procedure should explain how to replace or revoke it, update the server configuration, and verify that the new value works without printing it. Write down who owns that procedure before an incident occurs. For a personal app, that may be you. The broader lesson is that deployment is an ongoing responsibility: source, configuration, permissions, and data must agree. An AI assistant can help inspect and automate these steps, but it should never need your secret pasted into a public chat or committed file."] },
    ],
    takeaway: "Keep provider secrets on the server, enforce ownership for every operation, and verify the actual deployed configuration and access behavior.",
    reflection: "If a service key were exposed today, would you know where to replace it and how to confirm the replacement?",
    challenge: "Optional · 5 minutes: write a private-release note listing the source version, configuration names, allowed audience, two access checks, and recovery owner. Include no secret values.",
    quiz: { question: "The frontend hides a transcription button from non-owners. What else is necessary?", options: ["Nothing; hidden buttons secure the API", "The server must enforce ownership and keep the provider credential private", "Put the key in a less obvious browser variable"], answer: 1, explanation: "A caller can bypass interface controls. The server must verify permission before using its protected provider credential." },
    sources: [s.secrets, s.access], review,
  },
  {
    id: "plan-small-feature", version: 1, courseId: "ai-development", order: 8, title: "Plan a small feature", minutes: 5,
    objective: "Create a compact implementation brief that connects a user need to data, permissions, failure handling, review, and release checks.",
    sections: [
      { title: "Choose a feature you can finish", sources: [s.small.id], paragraphs: ["You have traced requests, handled uncertain saves, supplied coding context, reviewed changes, debugged failures, and considered deployment. Now combine those habits in a small feature brief. Our fictional Fieldnotes owner wants to pin a note so it is easier to find later. Google's small-change guidance provides the starting principle: aim for a self-contained change that leaves the system working. Pinning one existing note is a more useful first target than building an entire recommendation engine. State the user problem in one sentence: an important note gets buried, so the owner needs a simple way to keep it near the top of the list."] },
      { title: "Write the behavior before the solution", sources: [s.context.id], paragraphs: ["GitHub's prompting guidance emphasizes specific requirements and examples. Give the assistant a concrete case: when the owner pins a note, it appears in a clearly labeled pinned group; unpinning returns it to the ordinary ordering without changing its text. Decide whether several notes can be pinned and how they are ordered. Specify what happens when a pinned note is archived. These decisions are small, but leaving them implicit lets the implementation invent product behavior. You can label an unresolved preference as a question, while still defining enough scope for useful progress. The brief should make the intended outcome clear to someone who has not followed your earlier conversation."] },
      { title: "Map the data and authority", sources: [], paragraphs: ["In one possible design, each note stores a pinned flag, and the existing owner-checked update path changes that flag. The interface reads the saved value rather than maintaining a separate private interpretation. Pinning should survive a reload and appear on the owner's other device. The action must not change the note's text or grant access to anyone else. These statements describe responsibilities, not a demand for a particular database library. Ask the assistant to inspect the current schema and reuse the project's established pattern where it fits. If it proposes a new service or a broad rewrite, require a clear connection to the feature's actual needs."] },
      { title: "Describe failure and repetition", sources: [], paragraphs: ["The owner taps Pin on a poor connection. A sensible request sets the desired state to pinned, instead of blindly toggling whatever state the server currently holds. Repeating a set-to-pinned request can preserve the intended result, while repeating a toggle could undo it. The exact implementation still needs to handle concurrent edits and ownership. Define what the screen shows while saving and how it recovers if the result is uncertain. Keep the note visible and avoid announcing success before the server confirms it. By including one awkward case in the brief, you encourage the assistant to design the action as a real operation rather than a decorative interaction."] },
      { title: "Choose evidence for acceptance", sources: [], paragraphs: ["Write a few checks tied to the promise. Pin a note, reload, and confirm its placement. Open the app on another authenticated session and confirm the saved state. Unpin it and check that its text remains identical. Verify the chosen archive behavior and reject an unauthorized update in the test environment. A test of the ordering rule may be useful, while a browser check can cover the interaction. Ask the assistant to report the checks it actually ran and any relevant limitation. This creates a reviewable completion standard without turning a small feature into an enormous testing project."] },
      { title: "End with an operating decision", sources: [], paragraphs: ["Before publishing, identify the source version, whether stored data needs a migration, and how to recover if the new ordering confuses the owner. For this example, a compatible pinned flag can remain in storage while the prior interface ignores it, but that assumption should be checked against the actual implementation. Your finished brief now includes the need, behavior, data, authority, recovery, and acceptance evidence. Those six ideas let you collaborate with an AI coding assistant even while you are learning the technical details. The aim is not to predict every line of code. It is to give the work a clear purpose and a trustworthy way to decide when it is done."] },
    ],
    takeaway: "A small feature brief connects the user outcome to saved state, authority, failure handling, and evidence that the released behavior works.",
    reflection: "Which part of your next feature is least defined: the user behavior, saved data, permissions, or recovery?",
    challenge: "Optional · 10 minutes: write a one-page brief for a feature you want. Include one normal example, one failure case, the smallest usable scope, and three acceptance checks.",
    quiz: { question: "Why might a set-to-pinned request be preferable to an unconditional toggle?", options: ["It automatically eliminates all concurrency bugs", "Repeating the same desired-state request need not reverse the user's intended result", "It avoids needing server permission checks"], answer: 1, explanation: "Desired-state operations can make retries safer. They still need correct storage behavior, ownership checks, and a policy for concurrent changes." },
    sources: [s.small, s.context], review,
  },
];
